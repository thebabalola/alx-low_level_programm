#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 *Main - The main will serve as the starting or entry point
 *
 *Description - This programs will print positive, negative or zero
 *
 *Return - should exit on 0 (as success)
 *
*/

int main (void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	if (n > 0) {
		printf ("%i is positive\n, n)
			} else if (
	return (0);
}
