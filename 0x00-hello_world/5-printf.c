#include <stdio.h>

/**
 * main - prints the world in 'printf'
 * Description: This C program prints what is held in the printf fumction
 * Return: 0 if properly exited (sucess).
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

