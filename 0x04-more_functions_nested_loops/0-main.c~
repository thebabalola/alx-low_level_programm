#include "main.h"
#include <stdio.h>

/**
 * main - check     code
 *
 * Return: should always return 0
 */
int main(void)
{
	char charact;

	charact = 'A';
	printf("%c: %d\n", c. _isupper(c));
	charact = 'a';
	printf("%c: %d\n", c. _isupper(c));
	return (0);
}
