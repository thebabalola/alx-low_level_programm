#include "main.h"
/**
 *_abs - Check Main
 * @n: An integre input
 *
 * Description: The function returns the absolute value of a num
 * Return: Absolut value of number n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
